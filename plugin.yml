name: NightFallRPG
main: com.devon1337.RPG.NightFallRPG
version: 0.2.2.0303
api-version: 1.15
author: Devon1337
commands:
   class:
      description: Opens NightFall's classes.
      usage: /<command>
   simulate:
      description: Simulates an active ability
      usage: /<command>
   level:
      description: read/write user level data.
      usage: /<command>
   spellbook:
      description: Opens the spell book.
      usage: /<command>
   roll:
      description: rolls a dice.
      usage: /<command>
   trade:
      description: Trade window.
      usage: /<command>
   nfparty:
      description: Trade window.
      usage: /<command>
   nfquest:
      description: Trade window.
      usage: /<command>
   nfdialog:
      description: Trade window.
      usage: /<command>
   fl:
      description: Trade window.
      usage: /<command>
   nftest:
      description: Trade window.
      usage: /<command>
   nfprint:
      description: Trade window.
      usage: /<command>